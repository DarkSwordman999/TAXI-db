-- Статистика по классам автомобилей
SELECT 
    класс,
    COUNT(*) AS количество,
    AVG(год_выпуска) AS средний_год,
    MIN(год_выпуска) AS мин_год,
    MAX(год_выпуска) AS макс_год
FROM АВТОМОБИЛИ
GROUP BY класс
ORDER BY класс;
