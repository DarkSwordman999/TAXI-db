-- ДЕМОНСТРАЦИЯ CROSS JOIN: все сочетания автомобилей и водителей
SELECT 
    А.код AS код_авто,
    А.марка || ' ' || А.модель AS автомобиль,
    В.код AS код_водителя,
    В.фамилия || ' ' || В.имя AS водитель
FROM АВТОМОБИЛИ А
CROSS JOIN ВОДИТЕЛИ В
ORDER BY А.код, В.код
LIMIT 10;
